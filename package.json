{
  "name": "axios-demo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "build": "node scripts/build.mjs",
    "lint": "node -e \"console.log('lint: no-op for scaffold step')\"",
    "qa": "npm run lint && npm run build"
  },
  "devDependencies": {}
}
